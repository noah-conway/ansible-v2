---
- name: "deb test"
  hosts: pvevmdeb
  remote_user: ansi

  tasks:
    - name: debug
      ansible.builtin.debug:
        var: ansible_facts['pci']

  roles:
    - role: base/deb-base
    #- role: sys/vm-virtio-mount
      #vars:
        #virtiofs_dir: "virtio-share"
        #mount_point: "/mnt/virtio_data"
    #- base/RHEL-base
   # - role: sec/firewalld
    - role: podman/podman
    - role: podman/quadlet
    #- podman/copyparty
#    - podman/it-tools
